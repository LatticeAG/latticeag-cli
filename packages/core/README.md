# @latticeag/core

In-process LatticeAG SDK. Typed stage methods over the same events and adapters as the CLI. Event replication is latticeag sync, not @latticeag/core. Hosted LexGateway is invite-only.
