/** Copied keys used by the bus later (D10). */
export const DEFAULT_REDACT_KEYS = [
  "authorization",
  "Authorization",
  "apiKey",
  "api_key",
  "token",
  "secret",
  "password",
  "rawText",
] as const;
